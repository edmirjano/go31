import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'al.gremza.bless',
  appName: 'Bless Frontier Peoples',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
